<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ webserver_site_title }}</title>
  <style>
    body { font-family: system-ui, sans-serif; background:#0f1b2d; color:#e6edf3;
           text-align:center; padding-top:12vh; margin:0; }
    code { background:#1c2c44; padding:2px 6px; border-radius:4px; }
  </style>
</head>
<body>
  <h1>{{ webserver_site_title }}</h1>
  <p>Host <code>{{ ansible_hostname }}</code> running
     {{ ansible_distribution }} {{ ansible_distribution_version }}.</p>
  <p>Configured by the <code>webserver</code> Ansible role.</p>
</body>
</html>
