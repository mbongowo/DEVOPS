---
# Mirrors ../../requirements.yml — Molecule's galaxy dependency reads the
# scenario-local file (relative paths from the ephemeral dir don't resolve).
collections:
  - name: community.general
    version: ">=10.0.0,<11.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.docker
    version: ">=3.10.0,<5.0.0"
