# Copy to .env and fill in. NEVER commit a real .env.

# Docker Hub (used by the Jenkins pipeline to push images)
DOCKERHUB_USER=your-dockerhub-username
DOCKERHUB_TOKEN=your-dockerhub-access-token

# App config
CODE_LEN=6
