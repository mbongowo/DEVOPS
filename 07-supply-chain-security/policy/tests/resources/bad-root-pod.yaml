apiVersion: v1
kind: Pod
metadata:
  name: bad-root
spec:
  containers:
    - name: app
      # No securityContext.runAsNonRoot — rejected by require-run-as-non-root.
      image: ghcr.io/mbongowo/devops/supply-chain-demo:1.0.0
